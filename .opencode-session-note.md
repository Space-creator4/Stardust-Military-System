# Stardust Session Note — UI re-theme (green/blue dark ops console)

> Read this FIRST next session. Companion files: `./.opencode-brief-ui.md` (full brief),
> `C:\Users\cassi\AppData\Local\Temp\tacui-sfejxsm0.5fm\eval` reports (if any).

## Status — what is DONE and VERIFIED (do NOT redo)
- `server/server.js` + `server/tactical.js`: country-scoped tactical payloads (units, objectives,
  threats) and bases for ALL viewers incl. admins — no admin bypass. `node --check` passes.
  Server changes are COMPLETE; do not touch server again.
- `client/tacview.css` (324→335 lines): complete dark green/blue "ops console" re-theme.
  Tokens confirmed defined in map.html (`--font-display`, `--font-mono`, `--font-body`).
  Purely additive, coerced output. THIS is the palette source of truth.
- `client/tacview-map.js`, `client/tacview-models.js`, `client/renderer.js`: previously verified
  edits (cache colors, typeKey fix, LOD/trail gating, broadcastTac scoping, admin-scope removal at
  map_register). All `node --check` clean as of last verified run.

## What REMAINS (the actual "fix all the UI" work — next session)
Re-skin the **standalone client HTML pages** to match tacview.css palette. Files, in order:
1. `client/map.html` — page chrome (header/controls/readouts already partly themed; align all
   inline style + new-inline blocks to palette tokens).
2. `client/admin.html`
3. `client/chat.html`
4. `client/order.html`
5. `client/settings.html`
6. `client/login.html`
7. `client/map.html` secondary overlays (tacview inspect/legend re-check)
8. `client/force.html`
9. `client/index.html`

## Palette (from tacview.css — the ONLY colors allowed)
- Base bg: `#060a10`-`#0a1420` slate-blues / glass `rgba(12,18,25,.88)`-`rgba(7,11,16,.94)`
- Accent GREEN (live/own/selection): `#5ee6a0` (phosphor) w/ faint `rgba(94,230,160,0.08-.12)` glow
- Accent BLUE (chrome/links/focus): `#38bdf8`/`#60a5fa`, hairline `rgba(148,184,255,0.1-.16)`
- THREAT (danger ONLY): `#ff6b57` / `#ff8f7d` — never decorative
- Text: `#e7eef6` hi, `#8a99a8` muted, `#5b6c7a` dim; uppercase labels letter-spacing 1.4-2.5px
- Fonts: `var(--font-display)`, `var(--font-mono)`, `var(--font-body)` (defined per page)
- Borders/panels: 1px hairline `rgba(148,184,255,0.1)`, rounded 8-10px, backdrop blur 12px

## Rules
- ADDITIVE only: don't remove existing classes/ids/scripts. Never touch server files.
- After EVERY edit: `node --check <file>` for JS; for HTML, verify inline JS block with
  `node --check` on an extracted temp file; then quick manual smoke of loaded page.
- Check every `.tac-*` class reference still exists before/after re-theme.
- Keep `window.TacView`/`stardustSend`/`map_register`/WS protocol untouched — those wire the
  (already country-scoped) server payloads.

## Evaluation
Run the evaluation loop from frontend-design skill: open the re-themed `map.html` (and admin/chat)
in a browser, screenshot desktop + mobile, confirm the "green/blue ops console" reads as one
cohesive dark system, verify bases/units/objectives show only own country (admin = own country too),
threats stay red/global. Write eval report to temp dir (see header).
