# Stardust UI re-theme — continuation brief (execute in a FRESH session)

Goal: apply the green/blue dark "ops console" theme (the one already in `client/tacview.css`)
uniformly across every client HTML page, wired to the country-scoped server payloads. Start FRESH —
do not resume any prior session. Verify everything; never trust memory.

## Reference palette (already live in client/tacview.css — copy these EXACTLY)
- Base bg: `#060a10` .. `#0a1420` slate-blue gradients
- Phosphor green accent: `#5ee6a0` (selection, live, own side)
- Electric blue accent: `#38bdf8` / `#60a5fa` (chrome, links, focus)
- Danger (threats ONLY): `#ff6b57` / `#ff8f7d`
- Muted text: `#8a99a8`; dim: `#5b6c7a`; body text: `#dbe7f0` / `#e7eef6`
- Glass panels: `rgba(10,15,21,0.85)` → `rgba(7,11,16,0.94)`, hairline borders `rgba(94,230,160,0.1)` /
  `rgba(56,189,248,0.1)`, inset top highlight `rgba(255,255,255,0.04)`
- Fonts (already global tokens): `var(--font-display)` Rajdhani, `var(--font-mono)`,
  `var(--font-body)`; HUD labels = uppercase, ~1.5-2.5px letter-spacing, 8-10px, display font.

## Files to update, in order (each: edit → `node --check` if JS touched → manual smoke)
1. `client/map.html` — page chrome: header bar, control buttons, info readouts, overlay legend stays
   but recolor to palette; ensure `map_register` path still sends per-country payload (already server-side).
2. `client/admin.html` — side nav, stat cards, tables, modal dialogs, status pills → palette.
3. `client/chat.html` — message log, input, member/roster panel, system messages → palette.
4. `client/order.html` — order form, priority chips, status rows → palette.
5. `client/settings.html` — settings cards, inputs, toggles → palette.
6. `client/login.html` — login card, inputs, button → palette.
7. `client/force.html` — roster/force tree, role columns → palette.

## Rules
- Server (`server/`) is DONE — country-scoped for everyone, admins included. DO NOT touch server files.
- Only edit CSS vars / inline styles / class skinning in each HTML. Never change DOM ids or the
  ws-protocol strings (`map_register`, `sim_telemetry`, `bases`, `units`, `objectives`, `threats`,
  `orders`, `country_leaders`, `identity`, `chat`).
- `node --check` every .js you touch; script blocks inside the html get checked by opening the page.
- Keep the "purely additive" fallback: if tacview scripts fail, the classic map still works.
- Do NOT add external resources (no new fonts/CDNs/images). The palette is inline CSS only.
- Keep files small and readable; run `node --check` after every JS edit, `git diff` to review before done.
